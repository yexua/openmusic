# 前端环境变量示例（可选）
#
# 用法（仅需 SEO / sitemap 时）：
#   1. 复制本文件为 .env.production
#      Windows: copy .env.example .env.production
#      Linux:   cp .env.example .env.production
#   2. 取消下行注释并改成你的公网 https 域名（不要末尾斜杠）
#   3. 在 client 目录执行 npm run build
#
# 本地开发 npm run dev 无需配置。
# 后端配置见 server/.env.example

# VITE_SITE_URL=https://your-domain.com
